
import 'package:flutter/material.dart';

class RehabScreen extends StatelessWidget {
  const RehabScreen({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
       child: Text("Rehab Screen"),
      )
    );
  }
}
